import styled from "styled-components";

export const AutoCompleteList = styled.div`
  position: absolute;
  top: 56px;
  right: 0;
  width: 100%;
  height: 120px;
  background: var(--light);
  border-radius: 4px;
`
